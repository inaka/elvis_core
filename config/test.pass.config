[ % this check passes
 {
   elvis,
   [
    {config,
     [#{dirs => ["../../_build/test/lib/elvis/test/examples"],
        filter => "**.erl",
        rules => [{elvis_style, line_length, #{limit => 800}]
       }]
    },
    {output_format, plain}
   ]
 }
].

